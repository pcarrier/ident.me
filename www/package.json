{
  "scripts": {
    "build": "html-minifier-terser --collapse-whitespace --remove-comments --minify-js --minify-css -o dist/index.html index.html",
    "deploy": "pnpm run build && xmit www.ident.me"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0"
  }
}
